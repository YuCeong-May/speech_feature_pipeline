name: speechfeat
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - ffmpeg
  - libsndfile
  - pip
  - pip:
      - numpy>=1.24,<2.0
      - pandas>=2.0
      - scipy>=1.10
      - librosa>=0.10
      - soundfile>=0.12
      - PyYAML>=6.0
      - tqdm>=4.66
      - opensmile>=2.5
      - praat-parselmouth>=0.4
      - scikit-learn>=1.3
